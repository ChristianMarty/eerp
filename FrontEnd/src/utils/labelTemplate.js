/**
 * @param {string} zplCode
 * @param {Object} data
 * @returns {Boolean}
 */
export function labelTemplate(zplCode, data) {
  /* for (const [key, value] of Object.entries(data)) {
    if (value != null) {
      zplCode = zplCode.replaceAll(key, value)
    } else {
      zplCode = zplCode.replaceAll(key, '')
    }
  }*/
  return zplCode
}
