CREATE TABLE `renderer_dataset` (
	`Id` INT(10) UNSIGNED NOT NULL AUTO_INCREMENT,
	`Name` TINYTEXT NOT NULL,
	`Description` TEXT NULL DEFAULT NULL,
	`Variables` TEXT NULL DEFAULT NULL,
	PRIMARY KEY (`Id`)
);